export interface UserInfo {
  city: string;
  district: string;
  address: string;
  phone: string;
}

export interface PurchaseInfo {
  phone: string;
  city: string;
  district: string;
  address: string;
  contractsChecked: boolean;
}
