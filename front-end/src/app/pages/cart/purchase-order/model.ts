export interface Contact {
  phone: string;
  city: string;
  district: string;
  address: string;
  contractChecked: boolean;
}

export interface UserInfo {
  city: string;
  district: string;
  address: string;
  phone: string;
}
