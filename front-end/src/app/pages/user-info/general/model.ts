export interface City {
  city: string;
  code: number;
  districts: string[];
}

export interface UserInfo {
  firstName: string;
  lastName: string;
  address: string;
  city: string;
  district: string;
  email: string;
  phone: string;
}
